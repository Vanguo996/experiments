syntax = "proto3";

// package van;

option go_package = "./";

service Van {
  rpc HelloVan(request) returns (response){}
}
message request {
  string name = 1;
  int32 age = 2;
}

message response {
  string result = 1;
}